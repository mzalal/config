#!/bin/bash

# Git
alias gl="git log --oneline"
alias gll="git log"
alias gs="git status"
alias ga="git add"
alias gaa="git add -A"
alias gc="git commit"

# SSH
alias ssh-primary="ssh-add -D && ssh-add ~/.ssh/id_ed25519"
alias ssh-secondary="ssh-add -D && ssh-add ~/.ssh/id_ed25519_secondary"
